# Assignment 1
# BMI Calculator 
# น้ำหนัก / ส่วนสูง * ส่วนสูง

#Input
# weight = float(input("กรุณาป้อนน้ำหนัก (kg) : "))
# height = float(input("กรุณาป้อนส่วนสูง (cm) : "))

# #Processing

# # CM to M
# height_m = height/100
# bmi = weight/(height_m*height_m)

# #Output
# print("BMI Calculator :",bmi)


#split() ตัดช่องคำ
#replact() แทนที่
# import json

# appDict = {
#   'name': 'messenger',
#   'playstore': True,
#   'company': 'Facebook',
#   'price': 100
# }
# app_json = json.dumps(appDict)

# price = 50
# txt = "Hello price is {} Dollar {}"
# print(txt.format(price))